<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Standalone portal content template for the "Medical &amp; Dental" quotation design.
        Not inherited from portal.portal_sidebar - it is t-call'd by
        portal_sale_order_switch.xml when sale_order.quotation_template_id points here.

        The Products table below is dynamic (sale_order.order_line, same pattern
        as the Non Profit Proposal template). Only the savings/benchmark-comparison
        callout further down is still a static placeholder - there is no field yet
        for "customer's current phone bill" to compare against.
    -->
    <template id="quotation_template_content_medical" name="Quotation Portal - Medical &amp; Dental">
        <style>

        .med-quote {
          --med-navy: #071a2f; --med-navy2: #0b2b50; --med-accent: #0d7e77; --med-accent-soft: #e8f7f5;
          --med-cyan: #43c8f2; --med-green: #32b980; --med-ink: #172338; --med-muted: #617086;
          --med-line: #dbe5ee; --med-soft: #f4f8fc; --med-shadow: 0 18px 52px rgba(7,26,47,.10);
          font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif;
          color: var(--med-ink);
        }
        .med-quote h1, .med-quote h2, .med-quote h3, .med-quote h4 { margin-top: 0; line-height: 1.15; letter-spacing: -.035em; }
        .med-muted { color: var(--med-muted); }

        .med-hero {
          position: relative; overflow: hidden; color: #fff;
          padding: 48px 40px; margin-bottom: 28px; border-radius: 24px;
          background: radial-gradient(circle at 80% 15%, rgba(13,126,119,.20), transparent 28%),
                      radial-gradient(circle at 15% 15%, rgba(31,111,235,.20), transparent 28%),
                      linear-gradient(145deg, #061426, #0b2b50 65%, #071a2f);
        }
        .med-hero-grid { display: grid; grid-template-columns: 1.2fr .8fr; gap: 36px; align-items: center; }
        .med-eyebrow {
          display: inline-flex; align-items: center; gap: 9px; padding: 8px 12px;
          border: 1px solid rgba(67,200,242,.30); border-radius: 999px;
          background: rgba(255,255,255,.06); color: #c8efff; font-size: .8rem;
          font-weight: 850; letter-spacing: .06em; text-transform: uppercase; margin-bottom: 18px;
        }
        .med-eyebrow::before { content: ""; width: 8px; height: 8px; border-radius: 50%; background: var(--med-cyan); box-shadow: 0 0 16px var(--med-cyan); }
        .med-hero h1 { font-size: clamp(1.9rem, 3.4vw, 2.9rem); margin-bottom: 14px; }
        .med-hero .med-lead { color: #d8e5f0; font-size: 1.05rem; max-width: 620px; }
        .med-hero-actions { display: flex; gap: 12px; flex-wrap: wrap; margin-top: 24px; }

        .med-btn {
          display: inline-flex; align-items: center; justify-content: center; gap: 8px;
          min-height: 46px; padding: 0 18px; border-radius: 12px; font-weight: 850;
          border: 1px solid transparent; transition: .2s ease; cursor: pointer;
        }
        .med-btn-primary { color: #fff !important; background: linear-gradient(135deg, var(--med-accent), #0b2b50) !important; box-shadow: 0 12px 28px rgba(0,0,0,.20); border-color: transparent !important; }
        .med-btn-secondary, .med-btn-light { color: #fff !important; border-color: rgba(255,255,255,.22) !important; background: rgba(255,255,255,.06) !important; }
        .med-btn:hover { transform: translateY(-2px); }

        .med-proposal-card {
          padding: 26px; border-radius: 20px; border: 1px solid rgba(255,255,255,.14);
          background: rgba(4,17,31,.58); box-shadow: 0 20px 60px rgba(0,0,0,.18);
        }
        .med-proposal-card h3 { color: #fff; margin-bottom: 16px; font-size: 1.15rem; }
        .med-proposal-meta { display: grid; gap: 10px; }
        .med-proposal-meta > div { padding: 12px 14px; border-radius: 12px; background: rgba(255,255,255,.06); }
        .med-proposal-meta span { display: block; color: #9fb7c9; font-size: .74rem; text-transform: uppercase; letter-spacing: .06em; font-weight: 800; }
        .med-proposal-meta strong { display: block; color: #fff; margin-top: 4px; }
        [data-id="total_amount"] { display: inline-flex !important; align-items: baseline; gap: 2px; white-space: nowrap; }
        [data-id="total_amount"] * { display: inline !important; }

        .med-section-label { color: var(--med-accent); font-size: .78rem; font-weight: 900; letter-spacing: .10em; text-transform: uppercase; margin-bottom: 8px; }

        .med-card { padding: 28px; border-radius: 20px; background: #fff; border: 1px solid var(--med-line); box-shadow: 0 12px 38px rgba(7,26,47,.06); margin-bottom: 24px; }
        .med-intro-grid { display: grid; grid-template-columns: 1.05fr .95fr; gap: 40px; align-items: start; }
        .med-callout { padding: 26px; border-radius: 20px; border: 1px solid var(--med-line); background: linear-gradient(145deg, var(--med-accent-soft), #ffffff); box-shadow: 0 14px 40px rgba(7,26,47,.05); }
        .med-callout strong { display: block; font-size: 1.2rem; margin-bottom: 8px; }

        .med-priority-grid, .med-workflow-grid, .med-apps-grid, .med-metrics-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-top: 10px; }
        .med-icon { width: 42px; height: 42px; display: grid; place-items: center; border-radius: 12px; background: var(--med-accent-soft); color: var(--med-accent); font-weight: 950; margin-bottom: 14px; }
        .med-inner-card { padding: 24px; border-radius: 18px; background: #fff; border: 1px solid var(--med-line); }
        .med-inner-card p { color: var(--med-muted); margin-bottom: 0; }

        .med-phone-stack { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; margin-top: 10px; }
        .med-phone-card { padding: 22px; border-radius: 17px; border: 1px solid var(--med-line); background: linear-gradient(155deg,#fff,#f7fafd); }
        .med-phone-card span { display: inline-flex; padding: 6px 9px; border-radius: 999px; background: var(--med-accent-soft); color: var(--med-accent); font-size: .72rem; font-weight: 900; text-transform: uppercase; letter-spacing: .05em; margin-bottom: 12px; }
        .med-phone-card p { color: var(--med-muted); font-size: .89rem; margin-bottom: 0; }

        .med-dark-block { color: #fff; border-radius: 22px; padding: 34px; margin-bottom: 24px; background: linear-gradient(145deg, var(--med-navy), var(--med-navy2)); }
        .med-dark-block .med-section-label { color: var(--med-cyan); }
        .med-connection-flow { display: grid; grid-template-columns: repeat(6, 1fr); gap: 10px; margin-top: 20px; }
        .med-connection-flow div { padding: 14px 10px; border-radius: 12px; border: 1px solid rgba(255,255,255,.13); background: rgba(255,255,255,.06); text-align: center; font-size: .8rem; font-weight: 800; }

        .med-workflow-card { padding: 22px; border-radius: 17px; border: 1px solid var(--med-line); background: linear-gradient(155deg,#fff,#f7fafd); }
        .med-workflow-card h3 { font-size: 1.05rem; margin-bottom: 6px; }
        .med-workflow-card p { color: var(--med-muted); font-size: .89rem; margin: 0; }

        .med-safe-grid { display: grid; grid-template-columns: .9fr 1.1fr; gap: 22px; margin-top: 10px; }
        .med-safe-quote { padding: 26px; border-radius: 20px; color: #fff; background: linear-gradient(145deg, var(--med-navy), var(--med-navy2)); }
        .med-safe-quote strong { display: block; font-size: 1.4rem; line-height: 1.2; margin-bottom: 10px; }
        .med-safe-quote p { color: #c7d9e7; margin-bottom: 0; }
        .med-safe-controls { display: grid; gap: 10px; }
        .med-safe-control { padding: 16px 18px; border-radius: 14px; border: 1px solid var(--med-line); background: #fff; }
        .med-safe-control strong { display: block; margin-bottom: 4px; }
        .med-safe-control p { color: var(--med-muted); margin-bottom: 0; font-size: .87rem; }

        .med-app-card { min-height: 150px; padding: 20px; border-radius: 16px; border: 1px solid var(--med-line); background: linear-gradient(155deg,#fff,#f7fafd); }
        .med-app-card h4 { font-size: .96rem; margin-bottom: 6px; letter-spacing: -.02em; }
        .med-app-card p { color: var(--med-muted); font-size: .84rem; margin-bottom: 0; }

        .med-experience-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 14px; margin-top: 10px; }
        .med-experience-card { padding: 20px; border-radius: 15px; border: 1px solid rgba(255,255,255,.13); background: rgba(255,255,255,.06); }
        .med-experience-card span { display: block; color: var(--med-cyan); font-size: .74rem; font-weight: 900; text-transform: uppercase; letter-spacing: .06em; margin-bottom: 8px; }
        .med-experience-card p { color: #c2d5e4; margin: 0; font-size: .88rem; }

        .med-phase-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 14px; margin-top: 10px; }
        .med-phase-card { padding: 18px; border-radius: 16px; background: rgba(255,255,255,.06); border: 1px solid rgba(255,255,255,.12); }
        .med-phase-card span { display: block; color: var(--med-cyan); font-size: .76rem; font-weight: 900; letter-spacing: .06em; text-transform: uppercase; margin-bottom: 8px; }
        .med-phase-card p { color: #bed0df; margin: 4px 0 0; font-size: .86rem; }

        .med-metric-card { padding: 20px; border-radius: 16px; border: 1px solid var(--med-line); background: #fff; }
        .med-metric-card strong { display: block; font-size: .95rem; margin-bottom: 5px; }
        .med-metric-card p { color: var(--med-muted); font-size: .84rem; margin: 0; }

        .med-pricing-table { width: 100%; border-collapse: separate; border-spacing: 0; overflow: hidden; border: 1px solid var(--med-line); border-radius: 18px; box-shadow: 0 12px 40px rgba(7,26,47,.05); margin-top: 20px; }
        .med-pricing-table th, .med-pricing-table td { padding: 16px 18px; border-bottom: 1px solid var(--med-line); text-align: left; vertical-align: top; }
        .med-pricing-table th { background: var(--med-accent-soft); color: var(--med-navy); }
        .med-pricing-table tr:last-child td { border-bottom: none; }
        .med-pricing-table td:last-child { font-weight: 850; }
        .med-native-note { padding: 20px; border-radius: 16px; border: 1px solid var(--med-line); background: linear-gradient(145deg, var(--med-accent-soft), #fff); color: var(--med-muted); margin-bottom: 6px; }
        .med-native-note strong { color: var(--med-ink); }
        .med-totals { margin-top: 18px; display: grid; gap: 6px; max-width: 320px; margin-left: auto; }
        .med-totals-row { display: flex; justify-content: space-between; padding: 8px 0; }
        .med-totals-row span { color: var(--med-ink) !important; }
        .med-totals-total { font-weight: 900; font-size: 1.2rem; border-top: 1px solid var(--med-line); padding-top: 12px; }
        .med-totals-total span { color: var(--med-navy) !important; }

        .med-savings-clarification { margin-top: 22px; padding: 22px 24px; border-radius: 18px; border: 1px solid #bfe4d3; background: linear-gradient(145deg,#effbf6,#fff); }
        .med-savings-clarification h3 { margin: 0 0 6px; color: #115f43 !important; font-size: 1.1rem; }
        .med-savings-clarification p { margin: 0; color: var(--med-muted) !important; }
        .med-savings { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; margin-top: 16px; }
        .med-savings-card { padding: 22px; border-radius: 16px; border: 1px solid #bfe4d3; background: #fff; text-align: center; }
        .med-savings-card strong { display: block; color: #126243 !important; font-size: 1.8rem; margin-bottom: 8px; }
        .med-savings-card span { display: block; color: var(--med-ink) !important; font-size: .9rem; font-weight: 800; }
        .med-savings-card small { display: block; margin-top: 10px; padding: 9px; background: #f2f8f5; color: var(--med-muted) !important; border-radius: 10px; }
        .med-optional-note { margin-top: 16px; padding: 18px 20px; border-radius: 15px; border: 1px solid var(--med-line); background: #f8fafc; color: var(--med-muted); }
        .med-optional-note strong { color: var(--med-ink); }

        .med-cta { color: #fff; border-radius: 24px; padding: 40px; margin: 8px 0 24px; background: radial-gradient(circle at 80% 25%, rgba(13,126,119,.20), transparent 28%), linear-gradient(145deg, #071a2f, #0b3159); }
        .med-cta-grid { display: grid; grid-template-columns: 1fr auto; gap: 26px; align-items: center; }
        .med-cta p { color: #c6d7e5; max-width: 640px; margin: 10px 0 0; }

        .med-footer { background: #04111f; color: #9db3c4; padding: 22px 26px; font-size: .86rem; border-radius: 18px; display: flex; justify-content: space-between; gap: 16px; flex-wrap: wrap; }

        .med-quote #sale_order_portal_sidebar {
          color: #fff;
          background: radial-gradient(circle at 85% 10%, rgba(13,126,119,.20), transparent 32%), linear-gradient(150deg, var(--med-navy), var(--med-navy2));
          border-radius: 22px; border: 1px solid rgba(255,255,255,.12); box-shadow: var(--med-shadow); padding: 8px;
        }
        .med-side-total-label { display: block; text-transform: uppercase; letter-spacing: .08em; font-size: .78rem; font-weight: 800; color: #9fb7c9; margin-bottom: 4px; }
        .med-side-total-amount { color: #fff; font-size: 2rem; font-weight: 900; }
        .med-advantage { padding: 13px 15px; border-radius: 12px; background: rgba(255,255,255,.06); }
        .med-advantage-total { color: var(--med-cyan); }
        .med-contact-card { color: #fff; }
        .med-contact-card .med-muted { color: #9fb7c9; }

        /* The live site's theme forces its own text colors on p/h1-h4/span with enough
           specificity to beat inherited color, so text on our white cards is forced
           back to ink here (same fix used on the Modern Digital Workplace template). */
        .med-card h1, .med-card h2, .med-card h3, .med-card h4,
        .med-inner-card h3, .med-inner-card h4,
        .med-phone-card h3, .med-workflow-card h3, .med-app-card h4, .med-safe-control strong, .med-metric-card strong {
          color: var(--med-ink) !important;
        }
        .med-card p:not(.med-muted), .med-card div:not(.med-muted):not(.med-icon), .med-card span:not(.med-muted):not(.med-badge),
        .med-card strong, .med-card b, .med-card ul:not(.med-muted),
        .med-inner-card p:not(.med-muted), .med-phone-card p:not(.med-muted), .med-workflow-card p:not(.med-muted),
        .med-app-card p:not(.med-muted), .med-safe-control p:not(.med-muted), .med-metric-card p:not(.med-muted) {
          color: var(--med-ink) !important;
        }
        .med-muted { color: var(--med-muted) !important; }
        .med-pricing-table th { color: var(--med-navy) !important; }
        .med-pricing-table td { color: var(--med-ink) !important; }

        /* .med-safe-quote is a dark navy block nested inside the light .med-card
           "Keep What Works" section, so the ink-forcing rules above catch it too -
           restore its light text here with higher specificity to win back. */
        .med-quote .med-card .med-safe-quote,
        .med-quote .med-card .med-safe-quote strong {
          color: #fff !important;
        }
        .med-quote .med-card .med-safe-quote p {
          color: #c7d9e7 !important;
        }

        @media (max-width: 950px) {
          .med-hero-grid, .med-cta-grid, .med-intro-grid, .med-safe-grid { grid-template-columns: 1fr; }
          .med-priority-grid, .med-workflow-grid, .med-apps-grid, .med-metrics-grid,
          .med-phone-stack, .med-experience-grid, .med-connection-flow, .med-phase-grid { grid-template-columns: 1fr 1fr; }
        }
        /* True phone widths: the 2-column fallback above is still too
           cramped (cards clipped/overlapping) - drop to a single column. */
        @media (max-width: 640px) {
          .med-priority-grid, .med-workflow-grid, .med-apps-grid, .med-metrics-grid,
          .med-phone-stack, .med-experience-grid, .med-connection-flow, .med-phase-grid {
            grid-template-columns: 1fr;
          }
        }

        </style>

        <div class="med-quote">
          <div class="row o_portal_sale_sidebar">
            <t t-call="portal.portal_record_sidebar" id="sale_order_portal_sidebar">
              <t t-set="classes" t-value="'col-lg-4 col-xxl-3 d-print-none'"/>

              <t t-set="title">
                <span class="med-side-total-label">Total</span>
                <h2 t-field="sale_order.amount_total" data-id="total_amount" class="med-side-total-amount mb-0 text-break"/>
              </t>

              <t t-set="entries">
                <div class="d-flex flex-column gap-4 mt-3">
                  <div class="d-flex flex-column gap-2" id="sale_order_sidebar_button">
                    <a t-if="sale_order._has_to_be_signed()" role="button" class="btn med-btn med-btn-primary" data-bs-toggle="modal" data-bs-target="#modalaccept" href="#">
                      <i class="fa fa-check"/><t t-if="sale_order._has_to_be_paid()"> Sign &amp; Pay</t><t t-else=""> Accept &amp; Sign</t>
                    </a>
                    <a t-elif="sale_order._has_to_be_paid()" role="button" id="o_sale_portal_paynow" data-bs-toggle="modal" data-bs-target="#modalaccept" href="#" t-att-class="'btn med-btn %s' % ('med-btn-light' if sale_order.transaction_ids else 'med-btn-primary')">
                      <i class="fa fa-check"/> <t t-if="not sale_order.signature">Accept &amp; Pay</t><t t-else="">Pay Now</t>
                    </a>
                    <div class="o_download_pdf d-flex gap-2 flex-lg-column flex-xl-row flex-wrap">
                      <a class="btn med-btn med-btn-light o_print_btn o_portal_invoice_print flex-grow-1" t-att-href="sale_order.get_portal_url(report_type='pdf')" id="print_invoice_report" title="View Details" role="button" target="_blank"><i class="fa fa-print me-1"/>View Details</a>
                    </div>
                  </div>

                  <t t-if="not sale_order.is_expired and sale_order.state in ['draft', 'sent']">
                    <div t-if="sale_order.amount_undiscounted - sale_order.amount_untaxed &gt; 0.01" class="med-advantage" name="sale_order_advantage">
                      <small><b class="med-muted">Your advantage</b></small>
                      <small>
                        <b t-field="sale_order.amount_undiscounted" t-options="{&quot;widget&quot;: &quot;monetary&quot;, &quot;display_currency&quot;: sale_order.currency_id}" style="text-decoration: line-through" class="d-block mt-1" data-id="amount_undiscounted"/>
                      </small>
                      <t t-if="sale_order.amount_untaxed == sale_order.amount_total">
                        <h4 t-field="sale_order.amount_total" class="med-advantage-total" data-id="total_amount"/>
                      </t>
                      <t t-else="">
                        <h4 t-field="sale_order.amount_untaxed" class="med-advantage-total mb-0" data-id="total_untaxed"/>
                        <small>(<span t-field="sale_order.amount_total" data-id="total_amount"/> Incl. tax)</small>
                      </t>
                    </div>
                  </t>

                  <div t-if="sale_order.user_id" class="med-contact-card">
                    <h6><small class="med-muted">Your contact</small></h6>
                    <t t-call="portal.portal_my_contact">
                      <t t-set="_contactAvatar" t-value="image_data_uri(sale_order.user_id.avatar_128)"/>
                      <t t-set="_contactName" t-value="sale_order.user_id.name"/>
                      <t t-set="_contactLink" t-value="True"/>
                    </t>
                  </div>
                </div>
              </t>
            </t>

            <div id="quote_content" class="col-12 col-lg-8 col-xxl-9 mt-5 mt-lg-0">

              <!-- modals (unchanged from stock Odoo) -->
              <div role="dialog" class="modal fade" id="modalaccept" name="sale_order_modal_sign_and_pay">
                <div class="modal-dialog" t-if="sale_order._has_to_be_signed()">
                  <form id="accept" method="POST" t-att-data-order-id="sale_order.id" t-att-data-token="sale_order.access_token" class="js_accept_json modal-content js_website_submit_form">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <header class="modal-header">
                      <h4 class="modal-title">Validate Order</h4>
                      <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                    </header>
                    <main class="modal-body" id="sign-dialog">
                      <span>
                        By signing, you confirm acceptance on behalf of
                        <b t-field="sale_order.partner_id.commercial_partner_id"/>
                        for the <b data-id="total_amount" t-field="sale_order.amount_total"/> quote.
                      </span>
                      <b t-if="sale_order.payment_term_id" t-field="sale_order.payment_term_id.note"/>
                      <t t-call="portal.signature_form">
                        <t t-set="call_url" t-value="sale_order.get_portal_url(suffix='/accept')"/>
                        <t t-set="default_name" t-value="sale_order.partner_id.name"/>
                      </t>
                    </main>
                  </form>
                </div>

                <div class="modal-dialog" t-if="not sale_order._has_to_be_signed() and sale_order._has_to_be_paid()" name="sale_order_modal_validate">
                  <div class="modal-content">
                    <header class="modal-header">
                      <h4 class="modal-title">Validate Order</h4>
                      <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                    </header>
                    <main class="modal-body" id="sign-dialog">
                      <t t-set="prepayment_amount" t-value="sale_order._get_prepayment_required_amount()"/>
                      <t t-set="prepayment_available" t-value="sale_order.prepayment_percent and sale_order.prepayment_percent != 1.0"/>
                      <div t-if="prepayment_available" id="o_sale_portal_prepayment_buttons" class="d-flex btn-group mb-3" role="group">
                        <button name="o_sale_portal_amount_prepayment_button" class="btn btn-light active">
                          Down payment <br/>
                          <span t-esc="prepayment_amount" class="fw-bold" t-options="{'widget': 'monetary', 'display_currency': sale_order.currency_id}"/>
                        </button>
                        <button name="o_sale_portal_amount_total_button" class="btn btn-light">
                          Full amount <br/>
                          <span class="fw-bold" t-field="sale_order.amount_total"/>
                        </button>
                      </div>
                      <div class="mb-3">
                        <span t-if="prepayment_available">
                          <span id="o_sale_portal_use_amount_prepayment">
                            By paying a <u>down payment</u> of
                            <span t-esc="prepayment_amount" t-options="{'widget': 'monetary', 'display_currency': sale_order.currency_id}" class="fw-bold"/>
                            (<b t-esc="sale_order.prepayment_percent * 100"/>%),
                          </span>
                          <span id="o_sale_portal_use_amount_total">
                            By paying,
                          </span>
                        </span>
                        <span t-else="">
                          By paying,
                        </span>
                        you confirm acceptance on the behalf of <b t-field="sale_order.partner_id.commercial_partner_id"/>
                        for the <b data-id="total_amount" t-field="sale_order.amount_total"/> quote.
                        <b t-if="sale_order.payment_term_id" t-field="sale_order.payment_term_id.note" class="o_sale_payment_terms"/>
                      </div>
                      <div t-if="company_mismatch">
                        <t t-call="payment.company_mismatch_warning"/>
                      </div>
                      <div t-elif="not sale_order._has_to_be_paid()" class="alert alert-danger">
                        The order is not in a state requiring customer payment.
                      </div>
                      <div t-else="" id="payment_method" class="text-start mt-0">
                        <t t-call="payment.form">
                          <t t-set="sale_order_id" t-value="sale_order.id"/>
                        </t>
                      </div>
                    </main>
                  </div>
                </div>
              </div>

              <div role="dialog" class="modal fade" id="modaldecline">
                <div class="modal-dialog">
                  <form id="decline" method="POST" t-attf-action="/my/orders/#{sale_order.id}/decline?access_token=#{sale_order.access_token}" class="modal-content">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <header class="modal-header">
                      <h4 class="modal-title">Reject This Quotation</h4>
                      <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                    </header>
                    <main class="modal-body">
                      <p>Tell us why you are refusing this quotation, this will help us improve our services.</p>
                      <textarea rows="4" name="decline_message" required="" placeholder="Your feedback..." class="form-control"/>
                    </main>
                    <footer class="modal-footer">
                      <button type="submit" t-att-id="sale_order.id" class="btn btn-danger"><i class="fa fa-times"/> Reject</button>
                      <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Cancel</button>
                    </footer>
                  </form>
                </div>
              </div>

              <!-- status messages (unchanged from stock Odoo) -->
              <div t-if="message == 'sign_ok'" class="alert alert-success alert-dismissible d-print-none" role="status">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"/>
                <strong>Thank You!</strong><br/>
                <t t-if="message == 'sign_ok' and sale_order.state == 'sale'">Your order has been confirmed.</t>
                <t t-elif="message == 'sign_ok' and sale_order._has_to_be_paid()">Your order has been signed but still needs to be paid to be confirmed.</t>
                <t t-else="">Your order has been signed.</t>
              </div>
              <div t-if="message == 'cant_reject'" class="alert alert-danger alert-dismissible d-print-none" role="alert">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"/>
                Your order is not in a state to be rejected.
              </div>
              <t t-if="sale_order.get_portal_last_transaction()">
                <t t-call="payment.state_header">
                  <t t-set="tx" t-value="sale_order.get_portal_last_transaction()"/>
                </t>
              </t>
              <div t-if="sale_order.state == 'cancel'" class="alert alert-danger alert-dismissible d-print-none" role="alert">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="close"/>
                <strong>This quotation has been cancelled.</strong> <a role="button" href="#discussion"><i class="fa fa-comment"/> Contact us to get a new quotation.</a>
              </div>
              <div t-if="sale_order.is_expired" class="alert alert-warning alert-dismissible d-print-none" role="alert">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="close"/>
                <strong>This offer expired!</strong> <a role="button" href="#discussion"><i class="fa fa-comment"/> Contact us to get a new quotation.</a>
              </div>

              <div t-attf-class="#{'pb-5' if report_type == 'html' else ''}" id="portal_sale_content">

                <!-- Hero -->
                <div class="med-hero">
                  <div class="med-hero-grid">
                    <div>
                      <span class="med-eyebrow">Medical &amp; Dental Proposal</span>
                      <h1>Make every patient call easier to answer, route, document, and follow through.</h1>
                      <p class="med-lead">A complete business phone system for medical and dental offices—connected to appointment workflows, patient-service follow-up, staff coordination, communications, billing support, and approved business applications.</p>
                      <div class="med-hero-actions">
                        <a class="med-btn med-btn-primary" href="#pricing">Review Pricing</a>
                        <a class="med-btn med-btn-secondary" href="#applications">Explore Applications</a>
                      </div>
                    </div>
                    <aside class="med-proposal-card">
                      <h3>Proposal Overview</h3>
                      <div class="med-proposal-meta">
                        <div><span>Customer</span><strong t-field="sale_order.partner_id.name"/></div>
                        <div><span>Proposal</span><strong t-field="sale_order.name"/></div>
                        <div><span>Prepared by</span><strong t-field="sale_order.user_id.name"/></div>
                        <div t-if="sale_order.validity_date"><span>Valid until</span><strong t-field="sale_order.validity_date"/></div>
                        <div><span>Total</span><strong t-field="sale_order.amount_total" data-id="total_amount" t-options="{'widget': 'monetary', 'display_currency': sale_order.currency_id}"/></div>
                      </div>
                    </aside>
                  </div>
                </div>

                <!-- Executive Summary -->
                <div class="med-card">
                  <div class="med-intro-grid">
                    <div>
                      <div class="med-section-label">Executive Summary</div>
                      <h2 style="font-size:1.7rem;">Start with the phone. Improve the business around it.</h2>
                      <p>Calls remain one of the most important entry points into a medical or dental practice. Patients call to schedule, reschedule, ask for directions, reach the correct department, clarify nonclinical administrative questions, follow up on forms, and understand what happens next. A conventional phone system can route the call, but it does not normally connect the conversation to the work surrounding it. IPPBX begins with the complete phone system the practice already needs, then connects calls, SMS, voicemail, approved patient-service information, staff tasks, and business applications so fewer requests are lost between departments.</p>
                      <p class="med-muted">Every IPPBX phone customer can explore the complete AI Workforce during the first 90 days at no additional charge. The organization can test real workflows, train staff, and decide which applications create enough value to continue afterward.</p>
                    </div>
                    <div class="med-callout">
                      <strong>You did not just get a phone system.</strong>
                      <p class="med-muted">The phone becomes the entry point to customer or community context, call transcription, summaries, tasks, SMS, appointments, CRM, finance, workforce, reporting, and approved automation.</p>
                    </div>
                  </div>
                </div>

                <!-- Industry Priorities -->
                <div class="med-card">
                  <div class="med-section-label">Industry Priorities</div>
                  <h2 style="font-size:1.7rem;">What the connected phone system should improve</h2>
                  <div class="med-priority-grid">
                    <article class="med-inner-card"><div class="med-icon">01</div><h3>Patient Access</h3><p>Answer, route, transfer, schedule, and capture nonclinical patient requests with less waiting and repetition.</p></article>
                    <article class="med-inner-card"><div class="med-icon">02</div><h3>Appointment Continuity</h3><p>Connect calls, confirmations, rescheduling, reminders, cancellations, and approved follow-up.</p></article>
                    <article class="med-inner-card"><div class="med-icon">03</div><h3>Front-Desk Relief</h3><p>Reduce repetitive calls while preserving human handoff for sensitive, clinical, or exceptional needs.</p></article>
                    <article class="med-inner-card"><div class="med-icon">04</div><h3>Multi-Location Routing</h3><p>Route callers by location, department, provider, language, business hours, and approved rules.</p></article>
                    <article class="med-inner-card"><div class="med-icon">05</div><h3>Documented Follow-Through</h3><p>Turn conversations into summaries, tasks, callbacks, and accountable next actions.</p></article>
                    <article class="med-inner-card"><div class="med-icon">06</div><h3>Staff Coordination</h3><p>Connect phone activity with scheduling, HR, attendance, payroll, and management visibility.</p></article>
                  </div>
                </div>

                <!-- Complete Phone System -->
                <div class="med-card">
                  <div class="med-section-label">Complete Phone System</div>
                  <h2 style="font-size:1.7rem;">Every phone feature employees expect - then AI and business context on top.</h2>
                  <div class="med-phone-stack">
                    <article class="med-phone-card"><span>Calling &amp; Routing</span><h3>Reach the right person</h3><p>Auto attendants, extensions, ring groups, queues, business-hours routing, hunt groups, transfer, forwarding, and multi-location support.</p></article>
                    <article class="med-phone-card"><span>Apps &amp; Messaging</span><h3>Work from anywhere</h3><p>Desk phones, mobile and desktop apps, business SMS, voicemail to email, recording, presence, and the business number on approved devices.</p></article>
                    <article class="med-phone-card"><span>AI Calling</span><h3>Answer and follow through</h3><p>AI receptionist, inbound assistance, outbound business follow-up, human handoff, mid-call SMS, summaries, surveys, and approved next actions.</p></article>
                    <article class="med-phone-card"><span>Management</span><h3>See what is happening</h3><p>Call detail records, reporting, analytics, daily performance summaries, transcription, sentiment, role-based administration, integrations, and API access.</p></article>
                  </div>
                </div>

                <!-- One Connected Communication Story -->
                <div class="med-dark-block">
                  <div class="med-section-label">One Connected Communication Story</div>
                  <h2 style="font-size:1.6rem;">The call ends. The work continues.</h2>
                  <p style="color:#c7d9e7;">IPPBX can connect the conversation to the correct customer, patient-service, member, client, lead, account, or internal record - then preserve the meaning and start the next approved action.</p>
                  <div class="med-connection-flow">
                    <div>Caller Identified</div><div>Context Prepared</div><div>Call Handled</div><div>Summary Created</div><div>Action Assigned</div><div>Result Tracked</div>
                  </div>
                </div>

                <!-- Industry Phone Workflows -->
                <div class="med-card">
                  <div class="med-section-label">Industry Phone Workflows</div>
                  <h2 style="font-size:1.7rem;">Examples of one conversation becoming completed business action</h2>
                  <div class="med-workflow-grid">
                    <article class="med-workflow-card"><h3>A New Patient Calls</h3><p>Call answered → location and service identified → approved scheduling path opened → appointment or staff handoff completed → confirmation sent.</p></article>
                    <article class="med-workflow-card"><h3>A Patient Misses the Office</h3><p>Missed call detected → caller identified where possible → callback task created → approved SMS sent → outcome recorded.</p></article>
                    <article class="med-workflow-card"><h3>An Appointment Is Cancelled</h3><p>Cancellation recorded → approved waitlist or follow-up process started → available opening communicated → schedule updated.</p></article>
                    <article class="med-workflow-card"><h3>A Patient Needs the Right Department</h3><p>Reason understood → administrative, billing, scheduling, referral, or clinical team selected → call transferred with context.</p></article>
                    <article class="med-workflow-card"><h3>An After-Hours Call Arrives</h3><p>Approved after-hours instructions provided → non-emergency request documented → on-call or next-business-day routing applied according to practice rules.</p></article>
                    <article class="med-workflow-card"><h3>A Service Concern Is Raised</h3><p>Concern detected → summary created → manager or designated employee notified → resolution task tracked.</p></article>
                  </div>
                </div>

                <!-- Keep What Works -->
                <div class="med-card">
                  <div class="med-section-label">Keep What Works</div>
                  <h2 style="font-size:1.7rem;">Upgrade the phone system without unnecessarily changing how employees communicate.</h2>
                  <div class="med-safe-grid">
                    <div class="med-safe-quote"><strong>Same numbers. Familiar phones. Better capability underneath.</strong><p>The migration is planned around continuity: preserve numbers, recreate approved call flows, confirm compatible phones, test routing, train employees, and activate new capabilities at a controlled pace.</p></div>
                    <div class="med-safe-controls">
                      <div class="med-safe-control"><strong>Keep existing phone numbers</strong><p>IPPBX coordinates porting and validates account information before the move.</p></div>
                      <div class="med-safe-control"><strong>Reuse compatible SIP phones</strong><p>Phone models and firmware are reviewed before migration so there are no surprises.</p></div>
                      <div class="med-safe-control"><strong>Recreate current call flows first</strong><p>Greetings, menus, queues, ring groups, extensions, hours, and routing are documented and tested.</p></div>
                      <div class="med-safe-control"><strong>Test before cutover</strong><p>Internal contacts, temporary routing, and controlled testing are used before numbers move.</p></div>
                      <div class="med-safe-control"><strong>Activate AI and applications gradually</strong><p>Begin with the phone, then connect the applications that create measurable value.</p></div>
                    </div>
                  </div>
                </div>

                <!-- Business Applications -->
                <div class="med-card" id="applications">
                  <div class="med-section-label">Business Applications Around the Phone</div>
                  <h2 style="font-size:1.7rem;">Use the first 90 days to discover what should remain connected.</h2>
                  <p class="med-muted" style="max-width:900px">The phone plan opens the door to a connected AI Workforce. Applications can be explored, configured, tested, and expanded around the organization's real workflows. After the introductory period, keep individual capabilities, category bundles, or the complete platform according to the value created.</p>
                  <div class="med-apps-grid">
                    <article class="med-app-card"><h4>AI Phone &amp; Receptionist</h4><p>Answer common calls, understand the request, transfer to staff, schedule approved appointments, and begin follow-up.</p></article>
                    <article class="med-app-card"><h4>Appointment &amp; Scheduling Workflows</h4><p>Coordinate scheduling, confirmations, rescheduling, cancellations, reminders, and approved waitlist activity.</p></article>
                    <article class="med-app-card"><h4>Patient-Service CRM</h4><p>Keep nonclinical inquiry history, call outcomes, commitments, follow-up, and assigned responsibility connected.</p></article>
                    <article class="med-app-card"><h4>Email &amp; SMS Automation</h4><p>Send approved reminders, directions, forms, links, confirmations, and follow-up information.</p></article>
                    <article class="med-app-card"><h4>Website &amp; Search Automation</h4><p>Create service pages, connect forms, improve discoverability, and route inquiries into managed follow-up.</p></article>
                    <article class="med-app-card"><h4>Billing &amp; Accounting Support</h4><p>Connect approved billing questions, payment links, follow-up, invoicing, and accounting workflows.</p></article>
                    <article class="med-app-card"><h4>HR, Recruitment &amp; Onboarding</h4><p>Coordinate applicants, interviews, hiring, employee documents, training, and staff onboarding.</p></article>
                    <article class="med-app-card"><h4>Attendance &amp; Payroll</h4><p>Track schedules, attendance exceptions, approvals, and payroll-impacting issues.</p></article>
                    <article class="med-app-card"><h4>Inventory &amp; Purchasing</h4><p>Monitor approved supply levels, purchasing needs, and management alerts.</p></article>
                    <article class="med-app-card"><h4>AI Business Operations</h4><p>Surface unresolved patient-service, staffing, billing, scheduling, and operational issues requiring attention.</p></article>
                  </div>
                </div>

                <!-- 90-Day Experience -->
                <div class="med-dark-block">
                  <div class="med-section-label">Your 90-Day Experience</div>
                  <h2 style="font-size:1.6rem;">Do not guess what the applications could do. Use them.</h2>
                  <div class="med-experience-grid">
                    <article class="med-experience-card"><span>Weeks 1-2</span><h3 style="color:#fff;font-size:1rem;">Connect</h3><p>Confirm call flows, users, numbers, customer or community records, permissions, and priority workflows.</p></article>
                    <article class="med-experience-card"><span>Weeks 3-4</span><h3 style="color:#fff;font-size:1rem;">Configure</h3><p>Adapt approved scripts, routing, AI knowledge, SMS, CRM, scheduling, and follow-up to the organization.</p></article>
                    <article class="med-experience-card"><span>Months 2-3</span><h3 style="color:#fff;font-size:1rem;">Use &amp; Measure</h3><p>Run real workflows, train employees, review outcomes, improve rules, and identify the highest-value applications.</p></article>
                    <article class="med-experience-card"><span>After Day 90</span><h3 style="color:#fff;font-size:1rem;">Keep What Works</h3><p>Continue selected capabilities individually, through a category bundle, or as a complete AI Workforce platform.</p></article>
                  </div>
                </div>

                <!-- Implementation & Migration -->
                <div class="med-card">
                  <div class="med-section-label">Implementation &amp; Migration</div>
                  <h2 style="font-size:1.7rem;">A controlled move from current phone service to connected business action</h2>
                  <div class="med-phase-grid" style="background:none;">
                    <article class="med-phase-card" style="background:var(--med-accent-soft);border-color:var(--med-line);"><span style="color:var(--med-accent);">Phase 1</span><h3 style="font-size:1rem;color:var(--med-ink);">Discover</h3><p style="color:var(--med-muted);">Review numbers, carriers, phones, users, departments, call flows, business hours, applications, and priorities.</p></article>
                    <article class="med-phase-card" style="background:var(--med-accent-soft);border-color:var(--med-line);"><span style="color:var(--med-accent);">Phase 2</span><h3 style="font-size:1rem;color:var(--med-ink);">Design</h3><p style="color:var(--med-muted);">Recreate approved routing, queues, menus, extensions, permissions, SMS, recording, and escalation rules.</p></article>
                    <article class="med-phase-card" style="background:var(--med-accent-soft);border-color:var(--med-line);"><span style="color:var(--med-accent);">Phase 3</span><h3 style="font-size:1rem;color:var(--med-ink);">Build</h3><p style="color:var(--med-muted);">Provision the account, users, apps, phones, AI calling, integrations, and initial business workflows.</p></article>
                    <article class="med-phase-card" style="background:var(--med-accent-soft);border-color:var(--med-line);"><span style="color:var(--med-accent);">Phase 4</span><h3 style="font-size:1rem;color:var(--med-ink);">Test</h3><p style="color:var(--med-muted);">Validate incoming, outgoing, transfers, voicemail, SMS, reporting, failover, AI handoff, and key applications.</p></article>
                    <article class="med-phase-card" style="background:var(--med-accent-soft);border-color:var(--med-line);"><span style="color:var(--med-accent);">Phase 5</span><h3 style="font-size:1rem;color:var(--med-ink);">Port &amp; Launch / Optimize</h3><p style="color:var(--med-muted);">Coordinate porting, launch support, then review reports, workflows, and applications to keep after 90 days.</p></article>
                  </div>
                </div>

                <!-- Performance Scorecard -->
                <div class="med-card">
                  <div class="med-section-label">Performance Scorecard</div>
                  <h2 style="font-size:1.7rem;">Measure whether the phone system is improving real outcomes</h2>
                  <div class="med-metrics-grid">
                    <article class="med-metric-card"><strong>Calls answered and abandoned</strong><p>Baseline → pilot → target</p></article>
                    <article class="med-metric-card"><strong>Missed calls recovered</strong><p>Baseline → pilot → target</p></article>
                    <article class="med-metric-card"><strong>Appointments or callbacks created</strong><p>Baseline → pilot → target</p></article>
                    <article class="med-metric-card"><strong>Average answer and transfer time</strong><p>Baseline → pilot → target</p></article>
                    <article class="med-metric-card"><strong>Follow-up completed on time</strong><p>Baseline → pilot → target</p></article>
                    <article class="med-metric-card"><strong>After-hours outcomes</strong><p>Baseline → pilot → target</p></article>
                    <article class="med-metric-card"><strong>Department call volume</strong><p>Baseline → pilot → target</p></article>
                    <article class="med-metric-card"><strong>Patient-service issue escalation</strong><p>Baseline → pilot → target</p></article>
                  </div>
                </div>

                <!-- Commercial Proposal / Products (DYNAMIC - sale_order.order_line) -->
                <div class="med-card" id="pricing">
                  <div class="med-section-label">Commercial Proposal</div>
                  <h2 style="font-size:1.7rem;">Products</h2>

                  <table class="med-pricing-table">
                    <thead><tr><th>Products</th><th>Quantity</th><th>Unit Price</th><th>Amount</th></tr></thead>
                    <tbody>
                      <t t-foreach="sale_order.order_line" t-as="line">
                        <tr t-if="not line.display_type">
                          <td><span t-field="line.name"/></td>
                          <td>
                            <span t-field="line.product_uom_qty"/>
                            <span t-field="line.product_uom" groups="uom.group_uom"/>
                          </td>
                          <td><span t-field="line.price_unit" t-options="{'widget': 'monetary', 'display_currency': sale_order.currency_id}"/></td>
                          <td><span t-field="line.price_total" t-options="{'widget': 'monetary', 'display_currency': sale_order.currency_id}"/></td>
                        </tr>
                        <tr t-if="line.display_type == 'line_section'">
                          <td colspan="4"><strong t-field="line.name"/></td>
                        </tr>
                        <tr t-if="line.display_type == 'line_note'">
                          <td colspan="4"><i t-field="line.name"/></td>
                        </tr>
                      </t>
                    </tbody>
                  </table>
                  <div class="med-totals">
                    <div class="med-totals-row" t-if="sale_order.amount_tax">
                      <span>Taxes</span>
                      <span t-field="sale_order.amount_tax" t-options="{'widget': 'monetary', 'display_currency': sale_order.currency_id}"/>
                    </div>
                    <div class="med-totals-row med-totals-total">
                      <span>Total</span>
                      <span t-field="sale_order.amount_total" t-options="{'widget': 'monetary', 'display_currency': sale_order.currency_id}"/>
                    </div>
                  </div>

                  <!-- Savings - dynamic, computed from the two Studio fields on sale.order -->
                  <t t-set="ipx_current_monthly" t-value="sale_order.x_studio_monetary_field_4n_1junkim7e or 0.0"/>
                  <t t-set="ipx_ippbx_monthly" t-value="sale_order.x_studio_monetary_field_2gl_1jul3gsf0 or 0.0"/>
                  <t t-set="ipx_annual_savings" t-value="(ipx_current_monthly - ipx_ippbx_monthly) * 12"/>

                  <div class="med-savings-clarification">
                    <h3>Any savings shown are estimated savings - not additional charges.</h3>
                    <p>The organization pays the Odoo quotation total above. Comparison figures illustrate how much may remain in the budget versus current or benchmark phone spending.</p>
                  </div>
                  <div class="med-savings">
                    <div class="med-savings-card"><strong t-esc="'${:,.0f}'.format(ipx_annual_savings)"/><span>Estimated annual phone-service savings compared with the organization's current recurring cost</span><small><t t-esc="'${:,.2f}'.format(ipx_current_monthly)"/> current − <t t-esc="'${:,.2f}'.format(ipx_ippbx_monthly)"/> IPPBX) x 12 months</small></div>
                    <div class="med-savings-card"><strong>90 Days</strong><span>Complete AI Workforce access included with the phone plan during the introductory experience</span><small>Afterward, continue only the individual applications, bundles, or complete platform selected in Odoo.</small></div>
                  </div>

                  <div class="med-optional-note"><strong>Applications after the first 90 days:</strong> selected capabilities should be added as Odoo optional products or subscription lines. This keeps the phone proposal clear while allowing the customer to continue only the applications that produced meaningful value.</div>
                </div>

                <!-- Terms & Conditions / Payment terms -->
                <div class="med-card" t-if="sale_order.note or sale_order.payment_term_id">
                  <div class="med-intro-grid">
                    <div t-if="sale_order.note">
                      <div class="med-section-label">Terms &amp; Conditions</div>
                      <div t-field="sale_order.note"/>
                    </div>
                    <div t-if="sale_order.payment_term_id">
                      <div class="med-section-label">Payment Terms</div>
                      <p t-field="sale_order.payment_term_id.name"/>
                    </div>
                  </div>
                </div>

                <!-- Industry Responsibility -->
                <div class="med-card">
                  <div class="med-section-label">Industry Responsibility</div>
                  <h2 style="font-size:1.5rem;">Technology supports the organization's work - it does not replace professional judgment or accountability.</h2>
                  <div class="med-callout"><p class="med-muted" style="margin:0;">IPPBX supports communications and administrative workflows. It does not provide medical advice, diagnose conditions, replace clinical judgment, or determine emergency response. Clinical, privacy, recording, retention, and regulatory requirements must be reviewed and approved before deployment.</p></div>
                </div>
              </div>

              <!-- CTA / bottom sign actions -->
              <div t-if="sale_order._has_to_be_signed() or sale_order._has_to_be_paid()" class="med-cta d-print-none" name="sale_order_actions">
                <div class="med-cta-grid">
                  <div>
                    <div class="med-section-label" style="color:var(--med-cyan)">Odoo Portal Acceptance</div>
                    <h2 style="font-size:1.5rem;">Review, approve, and sign through Odoo.</h2>
                    <p>The final phone plan, extensions, setup, equipment, optional applications, taxes, validity, terms, online signature, and payment requirements are managed through Odoo.</p>
                  </div>
                  <div class="d-flex gap-2 flex-wrap">
                    <t t-if="sale_order._has_to_be_signed()">
                      <a role="button" class="med-btn med-btn-primary" data-bs-toggle="modal" data-bs-target="#modalaccept" href="#"><i class="fa fa-check"/><t t-if="sale_order._has_to_be_paid()"> Sign &amp; Pay</t><t t-else=""> Accept &amp; Sign</t></a>
                      <a role="button" class="med-btn med-btn-secondary" href="#discussion"><i class="fa fa-comment"/> Feedback</a>
                      <a role="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#modaldecline" href="#"> <i class="fa fa-times"/> Reject</a>
                    </t>
                    <a t-elif="sale_order._has_to_be_paid()" role="button" data-bs-toggle="modal" data-bs-target="#modalaccept" href="#" t-att-class="'med-btn %s' % ('med-btn-secondary' if sale_order.transaction_ids else 'med-btn-primary')">
                      <i class="fa fa-check"/> <t t-if="not sale_order.signature">Accept &amp; Pay</t><t t-else="">Pay Now</t>
                    </a>
                  </div>
                </div>
              </div>

              <!-- chatter -->
              <div class="med-card" id="sale_order_communication">
                <div class="med-section-label">Discussion</div>
                <h2 style="font-size:1.5rem;">Communication history</h2>
                <t t-call="portal.message_thread"/>
              </div>

              <!-- footer -->
              <div class="med-footer d-print-none">
                <span>Confidential quotation prepared for <t t-esc="sale_order.partner_id.name"/></span>
                <span t-esc="sale_order.company_id.name"/>
              </div>
            </div>
          </div>
        </div>
    </template>
</odoo>
